import { memo } from 'react';

export const RunHistoryPanel = memo(() => {
  return (
    <div className="rounded-lg border border-components-panel-border bg-white/80 p-4 text-sm text-neutral-600">
      TODO：运行记录列表（占位，待接入后端数据）
    </div>
  );
});

RunHistoryPanel.displayName = 'RunHistoryPanel';
